# Copilot instructions

The project instructions for coding agents are in `AGENTS.md` at the repo root
(tool-neutral, single source of truth). Read `AGENTS.md` before working in this repo —
it is the only file you need; this one carries no instructions of its own.
